# Node modules
node_modules/
frontend/node_modules/
backend/node_modules/

# Build artifacts
dist/
frontend/dist/
build/

# Real secret env files - NEVER commit
backend/.env
frontend/.env
frontend/.env.local
.env.local

# frontend/.env.production is SAFE to commit (only contains public API URL, no secrets)
# backend/.env is secret (contains DB password, JWT, Cloudinary keys) - kept above

# Keep example env files (safe to commit, no real secrets)
!.env.example
!backend/.env.example
!frontend/.env.example

# Tech-specific
.vscode/
.DS_Store
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.eslintcache
.stylelintcache

# TypeScript
*.tsbuildinfo

# OS related
Thumbs.db
ehthumbs.db
Desktop.ini
